<section class="about">
  <div class="container about__inner line-segment line-segment--start">

    <div class="line-segment line-segment--header">
      <img class="about__profile-pic" src="{{profile-pic.src}}" alt="{{profile-pic.alt}}" />
      {{>line-segment vertical=true class="line-segment__line--small-separator-header"}}
      {{>line-segment horizontal=true}}
    </div>

    <div class="">
      {{{body}}}
    </div>

    {{>line-segment vertical=true class="line-segment__line--small-separator"}}
  </div>
</section>
